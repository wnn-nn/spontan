import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ─── Core Brand Colors ──────────────────────────────────────────────────
  static const Color primary = Color(0xFFE11D48); // Brand Crimson
  static const Color primaryLight = Color(0xFFF43F5E); // Shadow & highlight
  static const Color background = Color(0xFFFFFFFF);
  static const Color onboardingBackground = Color(0xFFF9F9F9);
  static const Color surface = Color(0xFFFFFFFF);

  // ─── Typography Colors ─────────────────────────────────────────────────
  static const Color textHeading = Color(0xFF0B1C30); // Dark navy for prominent headers
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF565E74); // Slate grey for descriptions
  static const Color textMuted = Color(0xFF64748B);

  // ─── Accents, Dividers & Radiance ──────────────────────────────────────
  static const Color border = Color(0xFFE2E8F0);
  static const Color softGray = Color(0xFFF8FAFC);
  static const Color indicatorInactive = Color(0xFFDCE9FF);
  static const Color radianceCrimson = Color(0x1AB80035); // rgba(184, 0, 53, 0.1)
  static const Color radianceBlue = Color(0x66DAE2FD); // rgba(218, 226, 253, 0.4)
  static const Color success = Color(0xFF16A34A);
}
